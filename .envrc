# Database Configuration
export POSTGRES_USER=user
export POSTGRES_PASSWORD=password
export POSTGRES_DB=game_db
export DB_HOST=db
export DB_PORT=5432

# Backend API Configuration
export BACKEND_PORT=8080
export GIN_MODE=debug
